#include <stdio.h>
#include <stdlib.h>

//Aula 098
//Estrutura de dados homogênea do tipo array (vetor unidimensional)

int main() {

	/*
	Vetor (ou array) é um conjunto de elementos do mesmo tipo (por isso, homogêneo),
	sendo que esses elementos são armazenados de forma contígua (lado a lado, ou sequencialmente), na memória

	Cada elemento pode ser acessado utilizando-se o seu índice (posição no array).

	IMPORTANTE -> arrays iniciam no índice 0;
	*/

	int array[5] = {1, 2, 3, 4, 5}; //declarando e inicializando um array de inteiros

	return 0;
}