#include <stdio.h>
#include <stdlib.h>

//Aula 065
//For com decremento invés de incremento

int main() {

	for (int i = 9; i >= 0; i--)
		printf("%d\n", i);
		printf("Terminou o laco");
	return 0;
}